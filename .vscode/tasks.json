{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++.exe",
            "args": [
                "-g", "${workspaceFolder}/src/main.cpp",
                "-g", "${workspaceFolder}/src/master_mind_bot.cpp",
                "-o", "${workspaceFolder}/bin/MasterMind.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}